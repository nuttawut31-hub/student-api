# คำอธิบาย: เหตุใด Token ที่ถูกแก้ไขแม้เพียงตัวอักษรเดียวจึงถูกปฏิเสธ

JSON Web Token (JWT) ประกอบด้วย 3 ส่วนหลักที่คั่นด้วยเครื่องหมายจุด (.) ได้แก่:

1. **Header:** ระบุประเภท Token และอัลกอริทึมการเข้ารหัส (เช่น HMAC-SHA256)
2. **Payload:** ข้อมูล Claims ของผู้ใช้ (เช่น id, email, role, exp) ที่เข้ารหัสแบบ Base64Url
3. **Signature:** ลายมือชื่อดิจิทัลที่สร้างขึ้นเพื่อป้องกันการปลอมแปลง

### กลไกการตรวจสอบ Signature

ลายมือชื่อ (Signature) ถูกสร้างขึ้นมาจากสูตร:
`HMACSHA256(base64UrlEncode(header) + "." + base64UrlEncode(payload), secret)`

เมื่อ Client ส่ง Token มายังเซิร์ฟเวอร์ Middleware (`jwt.verify`) จะทำการตรวจสอบดังนี้:

1. นำส่วน **Header** และ **Payload** ที่แนบมากับคำขอ ไปคำนวณ Signature ใหม่อีกครั้งบนเซิร์ฟเวอร์ โดยใช้กุญแจลับ (`JWT_SECRET`) ที่เซิร์ฟเวอร์เก็บรักษาไว้เพียงผู้เดียว
2. นำ Signature ที่คำนวณได้ใหม่ ไปเปรียบเทียบกับ Signature ที่แนบมากับ Token

### เหตุผลที่ Token ถูกปฏิเสธ

เนื่องจากอัลกอริทึม Hash ทางวิทยาการเข้ารหัสลับมีคุณสมบัติ **Avalanche Effect** (การเปลี่ยนแปลงข้อมูลต้นทางแม้เพียงบิตเดียว จะทำให้ค่า Hash ที่ได้เปลี่ยนแปลงไปอย่างสิ้นเชิง)

หากผู้ไม่หวังดีหรือผู้ใช้แอบแก้ไขข้อมูลใน Payload (เช่น พยายามเปลี่ยน `role: "student"` เป็น `role: "admin"`) หรือแก้ไขส่วน Signature แม้เพียงตัวอักษรเดียว Signature ที่เซิร์ฟเวอร์คำนวณใหม่จะไม่ตรงกับ Signature ที่ส่งมา เซิร์ฟเวอร์จึงตรวจจับได้ทันทีว่า Token มีการถูกปลอมแปลงหรือแก้ไข และจะโยน Error ส่งผลให้ Middleware ตอบกลับเป็น `401 Unauthorized` พร้อมข้อความ `INVALID_TOKEN` ทันที
